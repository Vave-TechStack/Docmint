# ═══════════════════════════════════════════════════════════════
# DocMint - Production Deployment
# NGINX reverse proxy + Next.js application
# ═══════════════════════════════════════════════════════════════

version: '3.8'

services:
  # ─── Schema Sync Runner (one-shot) ───
  # Applies the Prisma schema to the live database before the app starts,
  # so schema changes (like the payments FK nullability fix) can't drift again.
  # Reuses the built image — it already contains the prisma CLI and schema.
  migrate:
    image: docmint:latest
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: docmint-migrate
    restart: "no"
    working_dir: /app/prisma
    command: ["npx", "prisma", "db", "push", "--skip-generate"]
    env_file:
      - ../.env
    environment:
      - NODE_ENV=production
    networks:
      - docmint-network

  # ─── Next.js Application ───
  nextjs:
    image: docmint:latest
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: docmint-nextjs
    restart: unless-stopped
    expose:
      - "3000"
    depends_on:
      migrate:
        condition: service_completed_successfully
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - RAZORPAY_KEY_ID=${RAZORPAY_KEY_ID}
      - RAZORPAY_KEY_SECRET=${RAZORPAY_KEY_SECRET}
      - RAZORPAY_WEBHOOK_SECRET=${RAZORPAY_WEBHOOK_SECRET}
      - AUTH_SECRET=${AUTH_SECRET}
    env_file:
      - ../.env
    volumes:
      - nextjs_cache:/app/.next/cache
    networks:
      - docmint-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health.txt"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # ─── NGINX Reverse Proxy ───
  nginx:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.nginx
    container_name: docmint-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/webhook-security.conf:/etc/nginx/webhook-security.conf:ro
      - nginx_cache:/var/cache/nginx
    depends_on:
      nextjs:
        condition: service_healthy
    networks:
      - docmint-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  docmint-network:
    driver: bridge

volumes:
  nextjs_cache:
  nginx_cache:
